{
  "name": "tablebite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @tablebite/server db:generate",
    "db:push": "pnpm --filter @tablebite/server db:push",
    "db:migrate": "pnpm --filter @tablebite/server db:migrate",
    "db:seed": "pnpm --filter @tablebite/server db:seed",
    "db:studio": "pnpm --filter @tablebite/server db:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
